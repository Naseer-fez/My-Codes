#include<ctype.h>


int Dateforamtchecker(char*date,int size){

for(int i=0;i<size;i++){

if(isdigit(date[i]))
{continue;}
else{

    return 0;
}}
//i need to check all the dates are valid or not , based on the format







return 1;

}